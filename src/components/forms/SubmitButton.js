// SubmitButton.js
import React from "react";
import { Button } from "@material-ui/core";

function SubmitButton() {
  return (
    <Button className="btn-form" variant="contained" color="primary" type="submit">
      Cadastrar
    </Button>
  );
}

export default SubmitButton;
